# openseedbox-common dependencies

require:
    - play [1.3.4,1.4[

repositories:
    - local:
       type: local 
       artifact: ${application.path}/extra-lib/[module]-[revision].jar
       contains:
          - local
